#!/usr/bin/env python3
"""A function that that transposes matrix"""


def np_transpose(matrix):
    """
    Transposes a matrix
    Parameters:
        - matrix (numpy.ndarray): a matrix to transpose
    Returns:
        numpy.ndarray: the transposed matrix
    """
    return matrix.transpose()
